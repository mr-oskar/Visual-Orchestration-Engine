kind = "web"
previewPath = "/"
title = "Servioen — Visual Data Engineering IDE"
version = "1.0.0"
id = "artifacts/servioen"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22601

[services.development]
run = "pnpm --filter @workspace/servioen run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/servioen", "run", "build" ]
publicDir = "artifacts/servioen/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22601"
BASE_PATH = "/"
